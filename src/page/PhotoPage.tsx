import React from 'react';
import PhotosList from '../components/PhotosList';

const PhotoPage = () => {
	return (
		<section>
			<h1>Photos</h1>
			<PhotosList />
		</section>
	);
};

export default PhotoPage;
